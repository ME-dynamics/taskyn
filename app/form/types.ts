export interface IFromProps {
  description: string;
  answer: string;
  type:string,
}
export interface IProgressProps{
  description: string;
  title: string;
  leftName:string,
  rightName:string,
  leftPercent:number,
}