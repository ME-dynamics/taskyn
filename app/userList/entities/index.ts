import { Providers } from "./providers";
import { CustomerRequestState } from "./customerRequest";

export const providersState = new Providers();

export const customerRequestState = new CustomerRequestState();
